# actionlint configuration.
# Declares custom labels for self-hosted and WarpBuild runner pools.
self-hosted-runner:
  labels:
    - macOS
    - ARM64
    - ram16
    - warp-macos-15-arm64-6x
    - warp-ubuntu-latest-x64-2x
